# Analysis outputs

The current result is:

```text
current/panel_batch=20260816T110610Z/
```

It contains the model table, zone coverage, regime summary and two figures.

Older runs are in `archive/`. They are kept for comparison, but they should
not be read as separate final models.
