# Analysis outputs

The repository contains early checks as well as the current cross-zone result.
The output to use for the latest discussion is:

```text
multi_zone_multi_vintage_v2/panel_batch=20260816T110610Z/
```

It contains the model table, zone coverage, regime summary and two figures.

Older directories are kept because they show how the analysis changed from a
single DE-LU run to a multi-vintage and then a multi-zone panel. They are not
all independent specifications.
